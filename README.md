# fail2ban_checker
Fail2Ban Email Reporter – A Python script that monitors active Fail2Ban bans and automatically sends email notifications, including a sorted IP list per jail and dynamic hostname in the subject line.
